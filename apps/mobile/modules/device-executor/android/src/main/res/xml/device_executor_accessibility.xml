<?xml version="1.0" encoding="utf-8"?>
<!-- canTakeScreenshot needs Android 11 (API 30); the module refuses to run
     below that. flagReportViewIds + flagIncludeNotImportantViews make the
     a11y_tree dump comparable with `uiautomator dump` on the adb track. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewFocused"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds|flagIncludeNotImportantViews|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:canTakeScreenshot="true"
    android:description="@string/device_executor_service_description"
    android:notificationTimeout="100" />
